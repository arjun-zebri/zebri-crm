/** Canonical outward-facing document frame — the single source of truth for
 *  how a sent document (invoice / contract / run sheet / questionnaire) and its
 *  previews are framed, matching the branding editor's builder canvas so what
 *  the MC builds is what couples receive. */

/** Max content width (px) of the document card, shared across every surface. */
export const DOC_MAX_WIDTH_PX = 720

/** Light-grey page canvas the white document card sits on. */
export const DOC_CANVAS_BG = '#F4F4F1'
