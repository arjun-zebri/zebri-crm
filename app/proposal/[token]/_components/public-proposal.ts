/**
 * Compatibility re-export — the shared proposal-view types + helpers
 * moved to `@/lib/payments/proposal-view` so the composer preview and
 * the branding editor can render the exact same layout as this public
 * page (one component, three surfaces).
 *
 * @module app/proposal/[token]/_components/public-proposal
 */
export * from '@/lib/payments/proposal-view';
