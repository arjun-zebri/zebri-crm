/**
 * Compatibility re-export: `Slider` moved to `components/editor`
 * (Proposal Layout v2 Phase 2, spec 5.3) so the Branding and proposal editors
 * share one set. Removed in Phase 5 with the Branding toolbar rebuild.
 *
 * @module app/(dashboard)/branding/components/slider
 */
export { Slider } from '@/components/editor'
